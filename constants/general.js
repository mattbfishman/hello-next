import create from '../configs/create';
import itemHeadings from '../configs/headings/items';

var pageConfigs = {
    create : create,
};

var tableHeadings =  {
    items  : itemHeadings
}

module.exports = {
    tableHeadings,
    pageConfigs
}